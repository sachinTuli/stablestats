1. Install node packages:
    
    $ npm install 

2. Run for DEVELOPMENT:
 
    $ npm start  
 
    This is will start a Node server as backend, so we can test on local easily. It's running on: 
    http://localhost:3001